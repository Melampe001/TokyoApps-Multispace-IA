# Vercel Serverless Functions - Python Dependencies
# Optimized for cold starts and bundle size

# CrewAI - AI agent orchestration (keep lightweight)
crewai==0.80.0
crewai-tools==0.12.0

# LLM API Clients (lightweight, official SDKs)
groq==0.13.0
openai==1.54.0
anthropic==0.25.0
google-generativeai==0.3.2

# HTTP client (httpx is faster than requests for async)
httpx==0.27.0

# Utilities (minimal dependencies)
python-dotenv==1.0.1
pydantic==2.9.2
